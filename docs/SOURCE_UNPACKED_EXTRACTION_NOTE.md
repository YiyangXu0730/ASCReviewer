# Source Unpacked Extraction Note

The canonical ZIP was safely extracted under `source_unpacked/` with the single top-level archive folder stripped.

`source_unpacked/.ascreview/context/ARTIFACT_INDEX_latest.json` was intentionally excluded because it is a stale continuation index and must not be used as the final artifact index.
